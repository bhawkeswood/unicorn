<!DOCTYPE html>
<html>

<head>
  <title>Unicorn Factory</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=EB+Garamond' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Playfair+Display:400,700italic' rel='stylesheet' type='text/css'>
</head>

<body>

  <div id="body">
    <% if current_user %>
      <div class="topframe">
        <ul id="menu-css">
          <li><%= link_to "Log out", logout_path %></li>
          <li><%= link_to "Home", user_path(current_user.id) %></li>
          <li><%= link_to "Add a Team", new_team_path %></li>
          <li><%= link_to "Teams", teams_path %></li>
          <li><%= link_to "Myplaneteers", users_path %></li>
          <li><div class="greeting">Logged in as <%= current_user.username %></div></li>
          <li><div class="avatar"><%= %></div></li>
          <li><%= link_to "Edit your profile", edit_user_path(current_user.id) %></li>
        </ul>
      </div>


        <% else %>
        <div class="topframe">
          <ul id="menu-css">
            <li><%= link_to "Sign up", signup_path %></li>
            <li><%= link_to "Log in", login_path %></li> 
            <li><%= link_to 'Login with Google', auth_at_provider_path(:provider => :google) %></li>
          </ul>
        </div>
        <% end %>
    </div>

    <div class="content">
      <div id="frame">
        <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, :id => "flash_#{name}" %>
        <% end %>
        <%= yield %>
      </div>
    </div>

    <div class="bottom">
      <div class="bottomtext">
        This is where our tickertape goes!
      </div>
    </div>

  </div>

</body>

</html>
