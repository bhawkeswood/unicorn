<h1>Current Users:</h1>

<div class="user_list">
  <ul class="user_list">
  <% @users.each do |user| %>
    <li><%= link_to "#{user.username}", user %></li>
  <% end %>
  </ul>
</div>

<%= link_to "Home", teams_path %>